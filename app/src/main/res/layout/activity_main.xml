<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FEFAF6">

    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/nav_host_fragment"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="80dp"
        android:clipToPadding="false"
        app:defaultNavHost="true"
        app:navGraph="@navigation/nav_graph" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigationView"
        android:layout_width="match_parent"
        android:layout_height="66dp"
        android:layout_gravity="bottom"
        android:background="#FEFAF6"
        app:backgroundTint="#FEFAF6"
        app:elevation="16dp"
        app:itemIconSize="24dp"
        app:itemPaddingTop="6dp"
        app:itemPaddingBottom="18dp"
        app:itemIconTint="@color/nav_item_colors"
        app:itemTextColor="@color/nav_item_colors"
        app:labelVisibilityMode="labeled"
        app:itemActiveIndicatorStyle="@style/TransparentIndicator"
        app:menu="@menu/bottom_nav_menu" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_add"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_add_white"
        android:contentDescription="Add new trip"
        app:backgroundTint="@color/fab_add_background"
        app:borderWidth="0dp"
        app:elevation="20dp"
        app:shapeAppearanceOverlay="@style/CircleFAB"
        android:outlineAmbientShadowColor="#F28522"
        android:outlineSpotShadowColor="#F28522"
        app:layout_anchor="@id/bottomNavigationView"
        app:layout_anchorGravity="top|center"
        android:translationY="14dp"
        app:fabCustomSize="64dp"
        app:maxImageSize="32dp"
        app:tint="#FFFFFF" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>